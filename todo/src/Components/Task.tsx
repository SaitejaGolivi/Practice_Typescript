import React, { useState } from "react";
import { ITask } from "../Model/Interface";

interface Props {
  task: ITask;
}
function Task({task}:Props) {

  return <div>Task</div>;
}

export default Task;
